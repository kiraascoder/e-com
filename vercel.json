{
  "functions": {
    "api/index.php": { "runtime": "vercel-php@0.7.4" }
  },
  "buildCommand": "composer install --no-dev --prefer-dist --optimize-autoloader && php artisan config:cache && php artisan route:cache && php artisan view:cache && npm ci && npm run build",
  "outputDirectory": "public",
  "routes": [
    { "src": "/build/(.*)", "dest": "/public/build/$1", "headers": { "cache-control": "public, max-age=31536000, immutable" } },
    { "src": "/storage/(.*)", "dest": "/public/storage/$1" },
    { "src": "/(.*)", "dest": "/api/index.php" }
  ]
}
