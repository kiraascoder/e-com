{
  "functions": {
    "api/index.php": { "runtime": "vercel-php@0.7.4" }
  },
  "installCommand": "php -r \"copy('https://getcomposer.org/installer', 'composer-setup.php');\" && php composer-setup.php && php composer.phar install --no-dev --prefer-dist --optimize-autoloader --no-interaction && npm ci",
  "buildCommand": "php artisan config:cache && php artisan route:cache && php artisan view:cache && npm run build",
  "outputDirectory": "public",
  "routes": [
    { "src": "/build/(.*)", "dest": "/public/build/$1", "headers": { "cache-control": "public, max-age=31536000, immutable" } },
    { "src": "/storage/(.*)", "dest": "/public/storage/$1" },
    { "src": "/(.*)", "dest": "/api/index.php" }
  ]
}
